# eslint-config-sq11y

This config is intended for Vue projects - enforcing best practices, accessibility and thorough documentation.

## Installation

```shell
pnpm install --save-dev eslint-config-sq11y
```

## Usage

Set your `eslint.config.js` to:

```js
export { default } from "eslint-config-sq11y";
```
